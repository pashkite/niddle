# GitHub Actions: Dashboard Preview

This workflow renders a static dashboard snapshot with Streamlit and publishes it to GitHub Pages.

- Triggered on pushes to `work` or manually via **workflow_dispatch**.
- Output is a static HTML page + screenshot in the `site/` artifact.
